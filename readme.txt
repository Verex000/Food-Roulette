To run this application, open Visual Studio 2019 and open the solution, run the application using Microsoft edge

We used ASP.Net to generate the web pages and Microsoft SQL Server for the SQL portion,
The Microsoft SQL Server Database is located locally in the solution.
It should be as simple as just opening the solution and running it.